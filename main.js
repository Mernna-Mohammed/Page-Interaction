function color(btn) {
    btn.style.backgroundColor = `rgb(${Math.random()*256},${Math.random()*256},${Math.random()*256})`
}

function area() {
    alert(`${window.innerHeight} x ${window.innerWidth}`)
}


